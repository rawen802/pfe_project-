---
- name: Deploy generated acl configs files to Cisco IOS devices
  hosts: all
  gather_facts: no
  connection: ansible.netcommon.network_cli

  tasks:
    - name: Push config file to device
      cisco.ios.ios_config:
        src: "../../output/acl_configs/{{ inventory_hostname }}_acl.cfg"
        save_when: modified